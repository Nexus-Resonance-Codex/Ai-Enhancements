# ============================================================
# nrc_ai — Python CI
# Tests NRC AI Enhancement PyTorch modules across
# Python 3.10, 3.11, 3.12 with the core `nrc` library.
# ============================================================
name: Python CI — nrc_ai library

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install PyTorch CPU
        run: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

      - name: Install NRC core library
        run: pip install "nrc @ git+https://github.com/Nexus-Resonance-Codex/NRC.git"

      - name: Install nrc_ai and dev tools
        run: pip install -e ".[dev]"

      - name: Lint with ruff
        run: |
          ruff check src/ --select E9,F63,F7,F82 --output-format=github
          ruff check src/ --exit-zero --output-format=github

      - name: Run tests with pytest
        run: pytest tests/ -v --tb=short

  coverage:
    name: Coverage Report
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install all dependencies
        run: |
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install "nrc @ git+https://github.com/Nexus-Resonance-Codex/NRC.git"
          pip install -e ".[dev]" pytest-cov
      - name: Run tests with coverage
        run: pytest tests/ --cov=src/nrc_ai --cov-report=term-missing --cov-report=xml
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
